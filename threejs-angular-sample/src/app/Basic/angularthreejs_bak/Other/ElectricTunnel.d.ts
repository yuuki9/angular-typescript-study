import * as THREE from 'three';
export declare class Models {
    constructor();
    static MakeTunnel(radius: number, height: number, tunnelTexturePath: string, planeTexturePath: string): THREE.Object3D;
}
